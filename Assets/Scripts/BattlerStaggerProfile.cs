namespace AWE.Synzza {
    public class BattlerStaggerProfile {
        public float DurationSeconds { get; private set; }

        public BattlerStaggerProfile(float durationSeconds) {
            DurationSeconds = durationSeconds;
        }
    }
}
